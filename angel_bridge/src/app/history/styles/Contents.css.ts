import { style } from '@vanilla-extract/css';

export const wrapper = style({
    display: 'flex',
    justifyContent: 'center',
    flexWrap: 'wrap',
    gap: '4rem',
    width: '104rem',
});
