import { style } from '@vanilla-extract/css'

export const banner = style({
    display: 'flex',
    width: '100vw',
    height: '12.5rem',
    justifyContent: 'center',
    marginTop: '5rem',
});